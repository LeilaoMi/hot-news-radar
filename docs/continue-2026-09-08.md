# continue-2026-09-08 — 工程改进批次进度与续接

> 本轮会话执行「项目评估报告」的改进建议。本文档是跨会话续接锚点：
> 接手者先读本文，再按「待办」施工，无需重读全部历史。

## 已完成（全部已推送 master，pytest 130 passed / ruff 零违规）

| Commit | 内容 | 对应建议 |
|---|---|---|
| `1ea1abd` | ①修复 `ai.analysis_model` 透传（loader 增 `ANALYSIS_MODEL` 键，env `AI_ANALYSIS_MODEL` 优先）；②crawler.yml 27 项 secrets 收敛 job 级；③新增 `core/logger.py`，AI/通知/爬虫 8 文件 279 处 print → log | P0-1/2/3 |
| `d05fac9` | ruff 配置 + lint.yml CI 门禁；存量违规清零（F541×56、F401×9、E722×3、E731×4、F841×4、E402×1） | P1-3、P1-4（裸 except 清零） |
| `9e36687` | 翻译缓存：`ai/cache.py`（SQLite，kind 含目标语言）+ translate/translate_batch 分流；13 个单测 | P1-5 |
| `e025a3f` | `tests/test_ai_config.py` 锁定 ANALYSIS_MODEL 透传行为（5 用例） | P1-1 |
| `6f8ea60` | 未完成工作审计补完：sqlite_mixin 逐行吞错修复、max_age_days 无效配置 warning、5 处降级注释 | 审计补完 |
| `cce750e` | HTML 渲染快照测试（场景 A 全区域富数据 / B 最小路径），黄金样本锁定输出字节 | P1-2 前置 |
| `85a2c42` | P1-2 步骤1：DOCTYPE+CSS 字面量 → `html_styles.py` `BASE_HEAD_HTML`（字节级一致） | P1-2 |
| `75aa6ea` | P1-2 步骤2：三个嵌套函数提升模块级（AST 自由变量检查 + 字符串状态机反缩进） | P1-2 |
| `97c1f6e` | P1-2 步骤3：按区域拆分 `_render_info_items/_render_error_section/_render_hotlist_section/_render_new_items_section`；尾块 JS → `html_script.py`；`render_html_content` 1727→122 行，html.py 3228→859 行 | P1-2 |
| `742f443` | P2-5：`gen_feed.py` 双产物（feed.xml + feed.json JSON Feed 1.1）；frequency.py maxsplit 弃用警告清零 | P2-5 |
| `693d872` | P2-1 语义去重：`ai/dedup.py` AIDeduplicator（规范化精确去重先行 + LLM 分批判定 + AICache kind=dedup + 静默降级），config `ai_dedup.enabled` 默认 false，`ai_dedup_prompt.txt`，13 单测 | P2-1 |
| `647d5bb` | P2-3 AI 日报：`report/ai_daily.py`（daily 轮次 AI 分析结果 → 独立日报页 output/html/ai-daily/，随 Publish 链路上线，不推送），config `ai_daily.enabled` 默认 true；站点导航条加 AI日报 入口（inject_nav 支持旧页升级），9 单测 | P2-3 |
| `0720f79` | P2-4 订阅生成器：`docs/subscribe.html` 纯静态页（10 话题点选 + 自定义词组 + 只读预览生成 frequency_words.txt 片段 + 复制/下载，草稿存 localStorage）；editor.html 加入口；Playwright(Edge) 8 项交互实测通过 | P2-4 |

## P1-2 已完成（2026-09-08 下午批次），拆分结果备忘

- `render_html_content`：< 200 行目标达成（实测 122 行）
- 模块格局：`html.py`（859 行，7 个函数）+ `html_styles.py`（1393 行，纯 CSS 常量）+ `html_script.py`（1035 行，页脚/JS 常量）
- 快照测试 `tests/test_html_snapshot.py` 长期保留：任何版式改动跑
  `HTML_SNAPSHOT_UPDATE=1 pytest tests/test_html_snapshot.py` 重新生成黄金样本后必须人工审查 diff
- 手术脚本（tmp_step1_css.py / tmp_step2_nest.py / tmp_step3_region.py，workspace 根）记录了
  字符串状态机反缩进、AST 自由变量检查、自底向上删除三套技法，可复用于其他大文件拆分

本地环境：venv `C:/Users/leilaomi/.workbuddy/binaries/python/envs/default`（Python 3.13 + 全依赖 + pytest + ruff）。
验证命令：`.../Scripts/python.exe -m pytest` 与 `-m ruff check .`（仓库根目录）。

## 待办 1：拆分 html.py（P1-2，未开工——已备施工图）

现状：`trendradar/report/html.py` 3228 行，`render_html_content` 单函数从第 16 行到文件尾，内含：
- 内联 CSS 大块（约 440–1000+ 行，含 `.section-divider`/`.hotlist-section`/`.ai-section` 等）
- 嵌套函数 `render_rss_stats_html`（1786）、`render_standalone_html`（1887）、`add_section_divider`（2160）

**安全前提（必须先做）**：快照测试。用 reports 分支的真实 HTML 作为黄金样本——
本地以相同输入数据重建 HTML 并与线上产物 diff 字节一致后，才允许动 html.py。
输入数据形状可从 `trendradar/report/data_preparer.py` 与 timeline 调度入口反推，
或先给 `render_html_content` 写特征化测试（characterization test）锁定输出。

**拆分步骤**（每步一 commit，每步后跑快照 diff）：
1. CSS → `trendradar/report/html_styles.py` 模块常量（纯字符串搬家，零逻辑风险）
2. 嵌套 `render_rss_stats_html` / `render_standalone_html` → 模块级函数（参数显式化）
3. `render_html_content` 按 region_order 分区（hotlist/rss/new_items/standalone/ai_analysis）
   拆为 `_render_region_*` 系列函数
4. 目标：主函数 < 200 行，各模块 < 500 行

## 待办 2：P1-4 长尾（渐进）

- 已完成：裸 except 清零（3/3）、analysis_service 升级 `log_exception`（ERROR 级 + DEBUG 堆栈）
- 剩余：161 处 `except Exception` 中 hot path（client.py 调用方、fetcher、senders 重试块）
  逐步改具体异常。原则：每次触碰一个模块时顺手收敛，不专项大批改。

## 待办 3：P2 新功能队列（评估报告第四节）

- ~~①多源同事件语义去重~~（P2-1 完成，`693d872`，默认关闭待用户开 `ai_dedup.enabled: true`）
- ~~③AI 日报自动生成~~（P2-3 完成，`647d5bb`，默认开启、不推送、随网页发布）
- ~~④关键词订阅 UI~~（P2-4 完成，`0720f79`，docs/subscribe.html 静态页）
- ~~⑤JSON Feed~~（P2-5 完成，`742f443`）
- 剩余：②热度趋势图表（reports 分支已有历史数据，`scripts/build_trends.py` 是雏形）
- 备注：语义去重/AI 日报上线后首个 Actions 周期需观察日志确认真实运行表现
  （AI 判定分组的准确率、日报页生成时机）；glm-4-flash 免费，无成本压力。
（`scripts/gen_feed.py` 已存在，扩展即可）⑥MCP 订阅管理工具。

## 本轮踩坑记录（复用价值）

- 批量 print→log 替换脚本对**跨行导入**（`from x import (`）会插错位置——已在
  filter_pipeline/dispatcher 两处手工修复；同类批量操作需先扫描 `^(from|import) .*[$]`
- ruff F401 会删除「看似未用」的模块级导入：本次 `__main__.py` 的 AIAnalyzer 导入
  确属死代码（真正使用点 analysis_service 有延迟导入），删除前已全库核对
- Git Bash 下同文件多个 Edit 并行会互相覆盖，必须串行
